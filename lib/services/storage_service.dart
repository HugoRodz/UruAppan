import 'dart:typed_data';

class StorageService {
  // Placeholder: implement upload using firebase_storage when needed.
  // Example signature below.
  Future<String> uploadBytes(Uint8List bytes, String path) async {
    // TODO: implement with FirebaseStorage.instance.ref(path).putData(bytes) and return download URL
    return Future.value('');
  }
}
